const accountURLs = {
  getMetamaskMessage: 'accounts/get_metamask_message/',
  metamaskLogin: 'accounts/metamask_login/',
};

const URL = {
  tokens: 'sales/tokens/',
  ...accountURLs,
};
export default URL;
